# Managed by chezmoi from .chezmoidata/packages.toml ([packages.mise]).
# Do not edit directly; edit the data file and run 'chezmoi apply'.

[tools]
{{ range .mise.tools.common -}}

{{- if eq $machine_type "personal"}}
{{ range .mise.tools.personal -}}

{{- if eq $machine_type "work"}}
{{ range .mise.tools.work -}}

{{ if eq .chezmoi.os "darwin" -}}
{{ range .mise.tools.darwin -}}

[packages]
{{ if and (eq .chezmoi.os "linux") (eq $machine_type "personal") -}}
{{ range .mise.packages.personal -}}

{{ if and (eq .chezmoi.os "darwin") (eq $machine_type "work") -}}
{{ range .mise.packages.work -}}
